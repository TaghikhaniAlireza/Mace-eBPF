# Default agent configuration (shipped with the mace-agent package).
# Edit paths as needed; restart the service after changes: systemctl restart mace

logging:
  path: /var/log/mace/events.log
  format: json

rules:
  path: /etc/mace/rules.yaml
