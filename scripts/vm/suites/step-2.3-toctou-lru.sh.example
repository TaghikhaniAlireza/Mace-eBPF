#!/usr/bin/env bash
# The production Step 2.3 suite is `step-2.3-toctou-lru.sh` (same directory, executable).
# Copy this file only if you want a private experimental variant — keep the `.example` suffix
# so `run-test.sh` skips it.

echo "Use ./step-2.3-toctou-lru.sh instead."
exit 0
